# Expo Router SSR POC

1. in one terminal run `run:server` - builds local and hosts the server created .next dir
2. in another terminal run bun start, then press i or a for env
3. play around

There are multiple ways to develop. live web, building web after changes. its dev choice at the moment
